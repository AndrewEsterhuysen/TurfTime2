namespace TurfTime2.Models;

public enum TeamViewMode
{
    Swipeable = 0,   // VIEW_A — Team list
    Rotation  = 1,   // VIEW_C — Next rotation pairs
    Field     = 2    // VIEW_B — Pitch + bench diagram (view-only default)
}
