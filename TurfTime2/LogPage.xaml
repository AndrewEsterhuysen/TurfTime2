<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TurfTime2.LogPage"
             Title="Log"
             BackgroundColor="#1b5e20">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Filter Controls -->
        <Grid Grid.Row="0" Padding="10" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <Picker x:Name="sessionPicker" 
                    Grid.Column="0"
                    Title="Select Game Session" 
                    TextColor="White"
                    TitleColor="White"
                    BackgroundColor="#2e7d32"
                    SelectedIndexChanged="OnSessionSelected" />
            
            <Button x:Name="refreshButton"
                    Grid.Column="1"
                    Text="??"
                    BackgroundColor="#FF6B35"
                    TextColor="White"
                    WidthRequest="50"
                    Clicked="OnRefreshClicked" />
        </Grid>
        
        <!-- Log Display WebView -->
        <WebView x:Name="logWebView" Grid.Row="1" />
        
        <!-- Action Buttons -->
        <Grid Grid.Row="2" Padding="10" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Button x:Name="exportButton"
                    Grid.Column="0"
                    Text="Export"
                    BackgroundColor="#FF6B35"
                    TextColor="White"
                    Clicked="OnExportClicked" />
            
            <Button x:Name="shareButton"
                    Grid.Column="1"
                    Text="Share"
                    BackgroundColor="#FF6B35"
                    TextColor="White"
                    Clicked="OnShareClicked" />
            
            <Button x:Name="clearButton"
                    Grid.Column="2"
                    Text="Clear All"
                    BackgroundColor="#f44336"
                    TextColor="White"
                    Clicked="OnClearClicked" />
        </Grid>
    </Grid>
</ContentPage>