<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
					xmlns:tools="http://schemas.android.com/tools">
	<application 
		android:allowBackup="true" 
		android:icon="@mipmap/appicon" 
		android:roundIcon="@mipmap/appicon_round" 
		android:supportsRtl="true"
		android:label="Turf Time"
		android:theme="@style/Maui.SplashTheme"
		android:usesCleartextTraffic="false"
		android:networkSecurityConfig="@xml/network_security_config">

		<!-- Disable Firebase Crashlytics collection -->
		<meta-data
			android:name="firebase_crashlytics_collection_enabled"
			android:value="false" />

		<!-- Do NOT declare base FirebaseMessagingService here — it steals MESSAGING_EVENT from
		     Plugin.Firebase.CloudMessaging's MyFirebaseMessagingService (merged via the NuGet AAR).
		     Only strip Crashlytics registrar and set default notification presentation. -->
		<meta-data
			android:name="com.google.firebase.components:com.google.firebase.crashlytics.CrashlyticsRegistrar"
			tools:node="remove" />

		<!-- Small (status bar) icon: white silhouette. Full-color app art is LargeIcon in FcmService. -->
		<meta-data
			android:name="com.google.firebase.messaging.default_notification_icon"
			android:resource="@drawable/ic_stat_turftime" />

		<meta-data
			android:name="com.google.firebase.messaging.default_notification_color"
			android:resource="@color/turftime_notification" />

		<!-- Must match FcmService.ChatChannelId and Cloud Function android.notification.channelId -->
		<meta-data
			android:name="com.google.firebase.messaging.default_notification_channel_id"
			android:value="turftime_chat" />

		
	</application>

	<!-- Remove AD_ID permission injected transitively by Firebase/Google Play Services measurement libraries.
			 This app does not use advertising features; FCM push notifications do not require it. -->
	<uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />

	<!-- Required Permissions -->
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />
	<uses-permission android:name="android.permission.VIBRATE" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
	<uses-permission android:name="android.permission.CAMERA" />

	<!-- Feature Declarations -->
	<uses-feature android:name="android.hardware.touchscreen" android:required="false" />
	<uses-feature android:name="android.hardware.wifi" android:required="false" />
	<uses-feature android:name="android.hardware.camera.any" android:required="false" />
</manifest>