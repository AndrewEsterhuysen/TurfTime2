{
  "name": "turftime-functions",
  "description": "Cloud Functions for Turf Time chat notifications",
  "version": "1.0.0",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0"
  },
  "private": true,
  "devDependencies": {
    "eslint": "^9.39.4",
    "eslint-config-google": "^0.14.0"
  }
}
